'use strict';
function settingsPage(){
 const refs=Object.entries(DATA.refs).filter(([k,r])=>state.referenceGroup==='all'||r.bucket===state.referenceGroup).sort((a,b)=>Object.keys(refLabels).indexOf(a[1].bucket)-Object.keys(refLabels).indexOf(b[1].bucket)||a[1].component.localeCompare(b[1].component,'pt-BR'));
 const rows=refs.map(([key,r],i)=>{const custom=Object.hasOwn(cfg.weekly,key),value=custom?cfg.weekly[key]:r.weekly;return `<tr><td>${esc(refLabels[r.bucket]||r.bucket)}</td><td><div class="component">${esc(title(r.component))}</div></td><td><label class="sr-only" for="weekly-${i}">Aulas semanais de ${esc(r.component)}</label><input class="reference-input ${value===null?'warning-outline':''}" id="weekly-${i}" data-reference="${esc(key)}" type="number" min="0" max="45" step="1" value="${value===null?'':value}" placeholder="—"><span class="reference-print hide">${moneyless(value)}</span></td><td>${badge(custom?'Ajustada pela escola':value===null?'Sem referência documental':'Referência incorporada',custom?'info':value===null?'warn':'good')}<div class="source-text">${esc(custom?'Carga semanal ajustada localmente; registre a fonte no controle da escola.':r.source)}</div></td></tr>`;}).join('');
 const conflicts=currentTeacherConflicts(),unresolved=conflicts.filter(teacherConflictUnresolved);const conflictRows=conflicts.map(r=>{const key=logicalKey(r),selected=cfg.teacherOverrides?.[key]||'';return `<tr><td><div class="component"><strong>${esc(r.shortClass)}</strong><small>${esc(title(r.componente))}</small></div></td><td>${(r.professorCandidates||[]).map(n=>`<span class="pill">${esc(title(n))}</span>`).join(' ')}</td><td><select data-teacher-override="${esc(key)}"><option value="">A confirmar</option>${r.professorCandidates.map(n=>`<option value="${esc(n)}" ${selected===n?'selected':''}>${esc(title(n))}</option>`).join('')}</select></td><td>${selected?badge('Responsável definido','good'):badge('Pendente','warn')}</td></tr>`;}).join('');
 const p=schoolProfile(),added=cfg.calendarAdded||[],removed=cfg.calendarRemoved||[],total=trimesterDays('1')+trimesterDays('2')+trimesterDays('3');
 return `<section class="card" id="settings-school"><div class="card-head"><div><div class="eyebrow" style="color:var(--blue);margin-bottom:7px">IDENTIFICAÇÃO DA UNIDADE</div><h2>${esc(schoolName())}</h2><p class="sub">Configuração local; não é enviada para a internet.</p></div>${badge(p.name?'Configurada':'Pendente',p.name?'good':'warn')}</div><div class="card-body"><div class="small-grid"><div class="field"><label for="school-name">Nome da escola</label><input id="school-name" value="${esc(p.name)}" placeholder="Ex.: EE Exemplo"></div><div class="field"><label for="school-code">Código</label><input id="school-code" value="${esc(p.code)}"></div><div class="field"><label for="school-city">Município</label><input id="school-city" value="${esc(p.city)}"></div><div class="field"><label for="school-sre">SRE</label><input id="school-sre" value="${esc(p.sre)}"></div><div class="field"><label for="school-responsible">Responsável pelo painel</label><input id="school-responsible" value="${esc(p.responsible)}"></div></div><div class="inline-actions" style="margin-top:16px">${btn(icon('save')+' Salvar identificação','save-school','','btn-primary')}</div></div></section>
 <section class="card" id="settings-calendar"><div class="card-head"><div><div class="eyebrow" style="color:var(--blue);margin-bottom:7px">CALENDÁRIO 2026</div><h2>Dias letivos efetivos usados no cálculo</h2><p class="sub">O modelo estadual pode ser adaptado a feriados municipais e recomposições homologadas.</p></div>${badge(total+' dias efetivos',total===200?'good':'warn')}</div><div class="card-body"><div class="quality-grid" style="margin:0"><article class="quality-item"><h3>1º trimestre</h3><div class="big number">${trimesterDays('1')} DL</div><p>04/02 a 20/05.</p></article><article class="quality-item"><h3>2º trimestre</h3><div class="big number">${trimesterDays('2')} DL</div><p>21/05 a 09/09.</p></article><article class="quality-item"><h3>3º trimestre</h3><div class="big number">${trimesterDays('3')} DL</div><p>10/09 a 18/12.</p></article></div><div class="formula"><strong>Previstas = A/S documentada × dias letivos efetivos ÷ 5</strong><br>Aproximação gerencial; para precisão por componente, confira o horário real.</div><div class="small-grid" style="margin-top:18px"><div class="form-block" style="margin:0"><h3>Excluir data do modelo</h3><p class="sub">Use para feriado/evento local não contemplado no modelo.</p><div class="field" style="margin-top:12px"><label for="calendar-remove-date">Data</label><input id="calendar-remove-date" type="date" min="2026-01-01" max="2026-12-31"></div><div class="inline-actions" style="margin-top:12px">${btn('Excluir data','calendar-remove')}</div></div><div class="form-block" style="margin:0"><h3>Adicionar recomposição</h3><p class="sub">Inclua apenas data aprovada/homologada pela escola/SRE.</p><div class="field" style="margin-top:12px"><label for="calendar-add-date">Data</label><input id="calendar-add-date" type="date" min="2026-01-01" max="2026-12-31"></div><div class="inline-actions" style="margin-top:12px">${btn('Adicionar data','calendar-add')}</div></div></div><p class="setting-note"><strong>Excluídas:</strong> ${removed.length?removed.map(d=>`<span class="pill">${esc(d)}</span>`).join(' '):'nenhuma'}<br><strong>Adicionadas:</strong> ${added.length?added.map(d=>`<span class="pill">${esc(d)}</span>`).join(' '):'nenhuma'}</p>${(added.length||removed.length)?`<div class="inline-actions" style="margin-top:10px">${btn('Limpar ajustes locais','calendar-clear','','btn-small')}</div>`:''}</div></section>
 <section class="card matrix-source-card" id="settings-matrix"><div class="card-head"><div><div class="eyebrow" style="color:var(--blue);margin-bottom:7px">MODALIDADE, COORTE E FONTE</div><h2>Referência curricular não é deduzida do DED</h2><p class="sub">Regular noturno, EMTI, cursos técnicos, coortes em continuidade e oferta bilíngue são dimensões distintas e podem se combinar.</p></div>${badge('Regra de segurança','info')}</div><div class="card-body"><div class="quality-grid" style="margin:0"><article class="quality-item"><h3>REG · noturno</h3><div class="big number">2026</div><p>Usa a matriz noturna aplicável em 2026.</p></article><article class="quality-item"><h3>EMTI · entrada 2026</h3><div class="big number">45 A/S</div><p>Referência da Resolução SEE nº 5.212/2025 quando aplicável à coorte.</p></article><article class="quality-item"><h3>EMTI · continuidade</h3><div class="big number">Fonte</div><p>Exige matriz/grade da coorte ou horário homologado. Sem fonte, fica sem previsão.</p></article><article class="quality-item"><h3>Oferta bilíngue</h3><div class="big number">Perfil</div><p>É identificada pelos componentes interculturais/bilíngues. A carga específica não é inferida por analogia.</p></article></div><p class="setting-note"><strong>Importante:</strong> blocos de Formação Técnica Específica sem componentes/cargas discriminados não autorizam completar valores por analogia.</p></div></section>
 <span id="settings-responsible" class="section-anchor-marker" aria-hidden="true"></span>${conflicts.length?`<section class="card"><div class="card-head"><div><h2>Professor responsável atual</h2><p class="sub">O consolidado usa o professor do 2º trimestre. Quando o próprio relatório traz dois nomes para a mesma turma e componente, o total é contado uma única vez e o responsável precisa ser confirmado.</p></div>${badge(`${unresolved.length} pendente(s)`,unresolved.length?'warn':'good')}</div><div class="table-wrap"><table class="data-table"><thead><tr><th>Turma / componente</th><th>Nomes encontrados no 2º trimestre</th><th>Responsável atual</th><th>Situação</th></tr></thead><tbody>${conflictRows}</tbody></table></div><div class="card-foot"><span>Selecionar um nome não altera o DED; apenas define quem aparece como responsável neste painel.</span></div></section>`:''}
 <div class="notice neutral">${icon('info')}<div class="text"><strong>Regra de consolidação:</strong> turma + componente forma um único diário lógico. Se o professor mudou entre trimestres, o consolidado atribui a responsabilidade ao professor presente no 2º trimestre. Duplicidades idênticas dentro do mesmo trimestre nunca são somadas duas vezes.</div></div>
 <section class="card"><div class="card-head"><div><h2>Carga semanal por turma curricular e componente</h2><p class="sub">Somente referências documentadas são incorporadas. Campos vazios permanecem fora do cálculo comparável.</p></div><div class="field"><label class="sr-only" for="reference-group">Matriz</label><select id="reference-group"><option value="all">Todas as matrizes</option>${Object.entries(refLabels).map(([k,v])=>`<option value="${k}" ${state.referenceGroup===k?'selected':''}>${esc(v)}</option>`).join('')}</select></div></div><div class="table-wrap reference-scroll"><table class="data-table"><thead><tr><th>Turma curricular</th><th>Componente</th><th>A/S</th><th>Origem</th></tr></thead><tbody>${rows||'<tr><td colspan="4" class="muted">Importe uma planilha para listar os componentes da escola.</td></tr>'}</tbody></table></div><div class="card-foot"><span>REG/NOITE e INT/INTEGRAL são validados na importação; curso técnico e perfil bilíngue são classificados separadamente.</span></div></section>
 <section class="card"><div class="card-head"><div><h2>Guardar e levar os ajustes</h2><p class="sub">Configurações e dados permanecem locais.</p></div></div><div class="card-body"><div class="inline-actions">${btn(icon('save')+' Salvar cópia HTML','save-html','','btn-primary')}${btn(icon('download')+' Exportar parâmetros','export-config')}${btn(icon('upload')+' Importar parâmetros','import-config')}</div><p class="setting-note">Use “Salvar cópia HTML” para levar junto as planilhas incorporadas e os ajustes locais. Não publique cópias preenchidas em repositório público.</p></div></section>`;
}
